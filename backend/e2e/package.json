{
  "name": "elwasys-backend-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end tests for the elwasys backend Portal (Vaadin Flow, Phase 3 AP6) - fachlicher Nachfolger von Portal/e2e (Vaadin 7), siehe docs/kb/06-ui-tests.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "smoke": "playwright test --config=playwright.smoke.config.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
