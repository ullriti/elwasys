# elwasys – Knowledge Base (KB)

Diese Knowledge Base ist der zentrale Ablageort für die Überarbeitung / Modernisierung
des elwasys-Projekts. Hier werden Recherche, Architektur, Datenmodell, Migrationsplan,
Teststrategie und die Remote-/Cloud-Init-Umgebung dokumentiert und fortlaufend gepflegt.

> Jede Arbeits-Session nutzt ihren eigenen Branch (`claude/...`); die KB auf dem
> jeweils aktuellsten Stand (master + gemergte PRs) ist die zentrale Wahrheit.
> Einstieg für neue Sessions: [/CLAUDE.md](../CLAUDE.md) → [05-migration-plan.md](05-migration-plan.md).

## Inhalt

| Dokument | Beschreibung |
|----------|--------------|
| [00-overview.md](00-overview.md) | Was ist elwasys? Zweck, Features, High-Level-Bild |
| [01-architecture.md](01-architecture.md) | Module, Komponenten, Kommunikationswege, Technologie-Stack |
| [02-data-model.md](02-data-model.md) | Datenbankschema (PostgreSQL), Entitäten, Beziehungen |
| [03-modules.md](03-modules.md) | Detaillierte Beschreibung der Module (Common, Client-Raspi, backend) |
| [04-build-and-run.md](04-build-and-run.md) | Build, Ausführung, Konfiguration, Deployment, CI |
| [05-migration-plan.md](05-migration-plan.md) | Modernisierungsplan: Rahmenbedingungen, Komponenten-Inventur, Zielarchitektur, Roadmap (lebendes Dokument) |
| [06-ui-tests.md](06-ui-tests.md) | UI-Test-Strategie für Client (JavaFX) und Portal (Vaadin) |
| [07-cloud-init.md](07-cloud-init.md) | Remote-/Cloud-Init-Umgebung zum Ausführen von Build & Tests |
| [08-test-plan.md](08-test-plan.md) | Testplan: Vertiefung der Frontend-Tests (Client & Portal) |

## Status-Log

| Datum | Ereignis |
|-------|----------|
| 2026-07-19 | KB angelegt, Projekt-Recherche & Übersicht erstellt |
| 2026-07-19 | Build in Remote-Umgebung verifiziert (Common install, Client compile/test-compile) |
| 2026-07-19 | SessionStart-Hook + portable cloud-config für Remote-Build/Tests erstellt & validiert |
| 2026-07-19 | Client-UI-Test-Harness (TestFX/Xvfb + JUnit5) aufgebaut; 2 headless Tests grün |
| 2026-07-19 | Portal-Build repariert (Vaadin/GWT/War/JDBC/API-Drift); `jetty:run` liefert Login-Seite |
| 2026-07-19 | Portal-E2E (Playwright, Node/TS) aufgebaut; Login-Smoke-Test grün (2/2) |
| 2026-07-19 | Client-E2E: echte App headless hochgefahren (fhem-Sim + DB) → SELECT_DEVICE; getDeconzServer-Bug gefixt |
| 2026-07-19 | Client-E2E vertieft (C2–C5): Geräteliste, Karten-Login, Gerät buchen, Programmstart; Suite 7/7 grün |
| 2026-07-19 | Portal-E2E vertieft (P3–P5): falsches Passwort, Logout, Navigation aller Admin-Views; Suite 5/5 grün |
| 2026-07-19 | Client-Login-Varianten (C6–C8) grün; Portal-CRUD (P6 Benutzer, P9 Gruppe) grün; Portal-Suite 7/7 |
| 2026-07-19 | Client C9 (zu wenig Guthaben) grün; Portal P10 (Gerät anlegen) grün; Portal-Suite 8/8 |
| 2026-07-19 | CI-Flakiness behoben (geteilte DB); Client C10 (Auto-Logout) + Portal P12 (Programm) grün; Client 12 / Portal 9 |
| 2026-07-20 | Client C12 (Abbruch) + Portal P8 (Guthaben) grün; Seeding auf postgres (FK-Cleanup inkl. credit_accounting); Client 13 / Portal 10 |
| 2026-07-20 | Client C11 (Auto-Ende) + Portal P7 (Benutzer sperren) grün; Client 14 / Portal 11 |
| 2026-07-20 | Client C16 (standortfremdes Gerät) + Portal P13 (Gruppe löschen) grün; Client 15 / Portal 12 |
| 2026-07-20 | Portal P15+P18 (Nicht-Admin-Frontend & Berechtigungen) grün; Portal 13 |
| 2026-07-20 | Client C14 (DYNAMIC-Preisanzeige) grün; Client 16. Verbleibende Fälle in 08-test-plan.md dokumentiert |
| 2026-07-20 | Client C13 (Resume) + C15 (DB-Ausfall→ERROR) grün; Client 18/18 |
| 2026-07-20 | Portal P14 (Standort), P16 (Passwort ändern), P17 (Einstellungen), P19 (Passwort vergessen), P20 (Gerätestatus) grün; Portal 18/18. Nur P21 (Cross-Component) offen |
| 2026-07-20 | Cross-Component P21/P22 (Wartungsverbindung Portal⇄Client: Log holen, Neustart, Status) grün; Client 21/21. Alle geplanten Tests umgesetzt |
| 2026-07-20 | **Phase 2 AP5 (Benachrichtigungsdienst SMTP/Pushover im Backend)**: `NotificationService` 1:1 portiert aus `ExecutionFinisher` (E-Mail via `spring-boot-starter-mail`, Pushover via selbstgeschriebenem `java.net.http`-Client, Formular-Form aus dem disassemblierten Alt-Bibliotheksbytecode hergeleitet); hinter `elwasys.notifications.enabled` (Default AUS, kein Doppelversand mit dem weiterhin selbst versendenden Client-Alt-Code) und noch von keinem produktiven Ablauf aufgerufen – Scharfschaltung folgt Phase 4. Vollständiges Alt-Notification-Inventar in kb/03-modules.md; elwaApp-Push und Portal-Passwort-E-Mails bewusst nicht portiert (Begründung dort). Wichtiger Fallstrick dokumentiert: `push_notification`-Spalte ist NICHT das Pushover-Opt-in. 11 neue Tests (GreenMail für E-Mail, JDK-`HttpServer`-Mock für Pushover), Backend-Suite **107/107** grün. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-20 | **Phase 2 AP6 (Deployment: Dockerfile, docker-compose, Helm Chart, TLS-Konzept) – letztes Arbeitspaket der Phase-2-Roadmap**: `backend/Dockerfile` (Multi-Stage, Build-Kontext = Repo-Wurzel, non-root Runtime `eclipse-temurin:21-jre-jammy`, `HEALTHCHECK` gegen `/actuator/health`) + `.dockerignore`; `deploy/compose/` (Backend + PostgreSQL 16, `.env.example`, optionales TLS-Overlay mit Caddy); `deploy/helm/elwasys-backend/` (Deployment/Service/Ingress/ConfigMap/Secret, `values.yaml` als Values-Dokumentation, bewusst ohne PostgreSQL-Sub-Chart – Begründung in kb/05); CI-Backend-Job baut das Image (kein Push), Release-Workflow veröffentlicht es zusätzlich nach GHCR mit dem eingebauten `GITHUB_TOKEN`. **Helm tatsächlich beschafft** (weder apt/snap noch get.helm.sh/GitHub-Releases über den Proxy erreichbar – über `go install` via `proxy.golang.org` gebaut) und `helm lint`/`helm template` grün verifiziert (nicht nur manuell geprüft); `docker compose config` für Basis- und TLS-Overlay grün; die im Dockerfile verwendeten mvn-Befehle lokal nachvollzogen (inkl. eines Reactor-Testaufbaus ohne Client-Raspi/Portal-Quellcode) und das gebaute Jar gegen eine frische PostgreSQL-Instanz gestartet (`/actuator/health` → UP). Backend-Suite weiterhin 107/107, Root-Reactor-Build weiterhin grün. Details in kb/05-migration-plan.md, kb/04-build-and-run.md |
| 2026-07-20 | **Phase 2 abgeschlossen (QA-Review ohne Befunde)**: Diff-Review aller Phase-2-Commits (137 Dateien, ~10.500 Zeilen) gegen CLAUDE.md/Roadmap, Stichproben-Tiefenprüfung kritischer Stellen (Auth-Hashing, Terminal-Token-Speicherung, Flyway-Migrationen, Standort-Scope, Benachrichtigungs-Flag-Gating, Deployment). Kein Alt-Modul verändert (`git diff a323c17..HEAD -- Common Client-Raspi Portal` leer). Alle Suiten grün: Backend 107/107, Root-Reactor-Build (4 Module), Client-UI/E2E 37/37, Cross-Component 3/3, Portal-E2E 18/18. Zusätzlicher Parallelbetriebs-Beweis: Backend-Flyway-Migrationen V1–V3 auf die geteilte E2E-DB `elwasys` angewendet, danach Client- und Portal-Suiten erneut vollständig grün. Deployment-Validierung (`docker compose config`, Helm lint/template) nachvollzogen. Phase 2 gilt damit als abgeschlossen; nächster Schritt: Phase 3 (Portal-Neubau). Details in kb/05-migration-plan.md |
| 2026-07-20 | **Phase 3 AP1 (Vaadin-Flow-Grundgerüst im Backend)**: `vaadin-spring-boot-starter` (Vaadin 24.10.8, `hilla`+`collaboration-engine` ausgeschlossen) in `backend/pom.xml`; Login-View (deutsche Texte 1:1 aus dem Alt-Portal), rollenbasierte Weiterleitung nach Login (`RootView`), Admin-Layout (`AppLayout`+`SideNav`: Dashboard/Benutzer/Benutzergruppen/Programme/Geräte/Standorte, Platzhalter-Views) und Benutzer-Layout (Übersicht, Platzhalter) über `SecurityConfig`s `VaadinSecurityConfigurer` abgesichert – Terminal-API-/WebSocket-Kette (AP4) unverändert. Zwei nicht offensichtliche Fallstricke gefunden und behoben (Vaadins Autokonfiguration bricht bestehende `webEnvironment=NONE`-Tests, `VaadinSecurityConfigurer` braucht einen echten `ServletContext`-Bean). **Befund**: Vaadin 24.10.x verlangt im Dev-Modus einen Online-Lizenzcheck gegen vaadin.com („Extended Maintenance"), den diese netzwerk-eingeschränkte Sandbox nicht bedienen kann – betrifft nicht die automatisierte Testsuite (läuft mit `vaadin.productionMode=true`), aber manuelles Ausprobieren/künftige Browser-E2E-Tests hier; als offene Frage an den Auftraggeber vorgemerkt (kb/05). 9 neue Tests (Sicherheits-Filterebene per MockMvc + reiner Reflection-Test der Rollen-Guard-Annotationen), Backend-Suite **116/116** grün. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-20 | **Phase 3 AP2 (Stammdaten-Views mit Feature-Parität)**: die 5 Admin-Platzhalter-Views (Benutzer, Benutzergruppen, Geräte, Programme, Standorte) haben jetzt echte Vaadin-Grid-Listen + modale CRUD-Dialoge (`backend/.../ui/admin/dialog/`), fachliche Nachfolger der Alt-Portal-`components/*Window`-Fenster inkl. Lösch-Wächtern (Programm/Standort noch referenziert → blockiert, Benutzergruppe → Benutzer werden umverteilt, 1:1 Alt-Code) und Benutzer-Sperren; Guthaben in der Benutzerliste ist reine Anzeige (Aufladen folgt in AP3). 5 neue Services (`UserService`, `UserGroupService`, `DeviceService`, `ProgramService`, `LocationService`) + 2 neue Exceptions, 19 neue Tests, Backend-Suite **135/135** grün. **De-Risking-Auftrag erledigt**: der Vaadin-Produktionsmodus-Build (`mvn package -Pproduction`) funktioniert in dieser Sandbox nachweislich (auch nach `mvn clean`), ein damit gestarteter Server liefert echte Vaadin-Seiten aus (Login-Formular, JS/CSS-Bundle, `/actuator/health` UP) trotz einer beim Start geloggten Lizenz-Fehlermeldung – das entschärft das AP1-Lizenzrisiko für AP6 technisch, lässt die rechtliche Frage aber offen. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-20 | **Phase 3 AP3 (Dashboard, Guthaben Aufladen/Historie, UsersDashboard)**: `AdminDashboardView` zeigt je Standort dessen Geräte mit „Frei"/„Besetzt" (Testfall P20, aus der laufenden Execution abgeleitet) plus Programm/Nutzer/Restzeit bei laufender Ausführung und einer Ausführungshistorie je Gerät – Datenbeschaffung im neuen, Vaadin-freien `DashboardService` (wiederverwendbar für die in AP5 geplanten Live-Updates). Guthaben aufladen (`CreditTopUpDialog`, Nachfolger `UserCreditWindow`, Testfall P8) und -Historie (`CreditHistoryDialog`, Nachfolger `CreditAccountingWindow`) sind über zwei neue Knöpfe in der Benutzerliste erreichbar und nutzen ausschließlich die bestehenden Phase-2-Methoden `CreditService#inpayment`/`#payout` (plus zwei neue reine Lese-Methoden `getAccountingEntries`/`getLastInpayment`) – Buchungen bleiben damit strukturell unveränderlich (`CreditAccountingEntryEntity` hat ohnehin keine Setter für gespeicherte Zeilen). `UserDashboardView` zeigt eigenes Guthaben + Buchungshistorie (Testfall P15), strukturell auf die eigenen Daten des angemeldeten Benutzers beschränkt (kein Pfad-/Query-Parameter). 9 neue Tests, Backend-Suite **144/144** grün, 0 Quelländerungen an Common/Client-Raspi/Portal. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-20 | **Offline-Buchungs-Konzept aufgenommen** (Auftraggeber-Wunsch nach Phase-2-Abschluss): Terminals sollen bei Backend-Ausfall für eine definierte Zeitspanne eigenständig Buchungen akzeptieren und nachmelden. Konzeptskizze (Snapshot, Offline-Entscheidung, Idempotenz-Journal, Replay, Zeitfenster, Konfliktbehandlung) + Entscheidung in kb/05; Phase-4-Roadmap-Punkt „Robustheit" erweitert; API-v1-Erweiterungsbedarf (Idempotenz/Replay/Snapshot-Endpunkte) für Phase-4-Beginn vorgemerkt |
| 2026-07-20 | **Phase 0 abgeschlossen** (Sicherheitsnetz steht); isolierte State-Machine-Charakterisierung + ElwaManager-DI nach Phase 1 verschoben (05-migration-plan.md) |
| 2026-07-20 | **Modernisierungsplan überarbeitet** (05-migration-plan.md): Rahmenbedingungen fixiert (Java-Backend, Postgres, Raspi-Terminals bleiben; Nutzerverhalten unverändert), vollständige Komponenten-Inventur, Zielarchitektur (zentrales Spring-Boot-Backend, Portal integriert, Terminal über API), Roadmap Phasen 1–5 |
| 2026-07-20 | **Phase 1 (Testframeworks)**: einzige TestNG-Testklasse `InactivitySchedulerTest` nach JUnit 5 migriert + von `src/main` nach `src/test` verschoben (lief zuvor nicht unter Surefire); TestNG- und ungenutzte JUnit-4-Dependency aus Client-Raspi/pom.xml entfernt |
| 2026-07-20 | **Phase 1 (Build-Teil)**: Aggregator-Parent-POM angelegt, Common auf Java 21 gehoben, Portal-Sprachlevel (1.8) bewusst eingefroren, CI/Release-Skripte auf den Parent-POM-Reactor umgestellt (`mvn -f pom.xml install -pl Common -am`), Release-Workflow nutzt jetzt `versions:set` statt sed-Hack; alle drei Module bauen grün, Details in 04-build-and-run.md + 05-migration-plan.md |
| 2026-07-20 | **Phase 1 (Client-Raspi Java 21 + ElwaManager-DI)**: Client-Raspi auf Java 21 gehoben, minimaler DI-Seam für `ElwaManager` eingeführt, 12 neue isolierte Charakterisierungstests für `MainFormStateManager` (JUnit 5, ohne TestFX/DB); volle Client-Suite grün (~~33/33~~ **37/37**, siehe QA-Review-Korrektur unten) |
| 2026-07-20 | **Phase 1 abgeschlossen (QA-Review)**: Diff-Review aller Phase-1-Commits gegen CLAUDE.md/Roadmap, DI-Seam und neue Tests verifiziert (isoliert, Produktionsverhalten unverändert). Volle Suiten grün: Client 37/37, Cross-Component 3/3, Portal-E2E 18/18, `mvn package`/`install` für alle drei Module grün (Portal-Bytecode als Java 8 verifiziert). **Zwei echte Regressionen gefunden und behoben**: CI/Release-Workflows liefen noch mit JDK 17 (inkompatibel mit dem neuen Sprachlevel 21 von Common/Client-Raspi) → auf JDK 21 angehoben; `setup.sh` installierte auf neuen Raspi-Terminals noch ein Java-17-JRE (das das jetzt mit Sprachlevel 21 gebaute fat-jar nicht ausführen könnte) → auf `bellsoft-java21-runtime-full` angehoben. Testzahl-Dokufehler (33/33 statt 37/37) korrigiert. Details in 05-migration-plan.md |
| 2026-07-20 | **Roadmap um Phase 6 „Produktivumschaltung” ergänzt** (Auftraggeber-Wunsch): Umbau des bestehenden Produktiv-Setups nach Phase 5 – Migrationsskripte, Terminal-Neuaufsetzung (Java-17-Thema final gelöst), Upgrade-Skript für Terminals mit optionalem Auto-Update+Rollback; Portal/Backend brauchen dank Docker/Kubernetes/Helm kein eigenes Upgrade-Skript, nur automatisierte Post-Deploy-Smoke-Tests. Details in 05-migration-plan.md |
| 2026-07-20 | **Phase 2 AP1 (Backend-Gerüst + Flyway-Baseline)**: neues Modul `backend` (Spring Boot 3.5.16 per BOM-Import, Java 21, Actuator-Health, JDBC+Flyway, kein JPA) im Root-Reactor; Flyway-Baseline `V1__baseline_schema_0_4_0.sql` aus `database-init.sql` erzeugt (Rollenanlage idempotent gefasst, Spaltentypo bewusst erhalten); `config.db.version`-Mechanismus untersucht (wird von keinem Java-Code gelesen) und offiziell stillgelegt – künftige Schemaänderungen laufen nur noch über Flyway. Schema-Äquivalenz (Alt-Weg vs. Flyway-Baseline) und `baselineOnMigrate` gegen eine Bestands-DB mit `backend/verify-schema-baseline.sh` verifiziert (schema-identisch bis auf Flywayss eigene Historientabelle). Tests: Testcontainers als Default, lokaler PostgreSQL-Override (`ELWASYS_TEST_JDBC_URL`) für die Docker-lose Sandbox-Umgebung, über `backend/run-backend-tests.sh` grün (2/2). Vierter CI-Job „Backend” (Testcontainers, Docker in GitHub Actions vorhanden). Root-Reactor-Build (alle vier Module) sowie isolierter Common/Client-Raspi/Portal-Build weiterhin grün, keine Quelländerung an den drei Altmodulen. Details in 05-migration-plan.md, 02-data-model.md, 04-build-and-run.md |
| 2026-07-20 | **Phase 2 AP2 (JPA-Entities/Repositories + Geschäftslogik-Portierung)**: `spring-boot-starter-data-jpa` ergänzt; 8 Entities (`UserGroupEntity`, `UserEntity`, `LocationEntity`, `DeviceEntity`, `ProgramEntity`, `ExecutionEntity`, `CreditAccountingEntryEntity`, `ConfigEntity`) 1:1 aufs Bestandsschema gemappt (n:m-Tabellen als `@ManyToMany`, Postgres-native Enums über `@JdbcTypeCode(NAMED_ENUM)`, alle fachlich genutzten Assoziationen EAGER – analog zum durchgängig eager ladenden Alt-`DataManager`); 6 Repositories; 4 Services (`PricingService`, `CreditService`, `PermissionService`, `ExecutionService`), jeweils 1:1-Portierung mit Alt-Code-Quellenverweis (Preisberechnung, Guthaben/Abrechnung, Berechtigungs-Matrix, Execution-Lebenszyklus). **Alt-vs-Neu-Vergleichstests** umgesetzt: `common` als test-scope-Dependency, `LegacyDataManagerFactory` baut eine echte Alt-`DataManager` gegen dieselbe Test-DB, `PricingServiceParityTest`/`CreditServiceParityTest` vergleichen Alt- und Neu-Berechnung bitgenau (Wert + `BigDecimal`-Skala) – deckte u. a. die `new BigDecimal(double)`-Fließkomma-Eigenheit und die skalasensitive `price.equals(BigDecimal.ZERO)`-Prüfung im Alt-Code auf, beides bewusst 1:1 nachgebildet (Details/weitere Beobachtungen in 05-migration-plan.md). 27/27 Backend-Tests grün (`backend/run-backend-tests.sh`); `run-backend-tests.sh` und der CI-Backend-Job bauen jetzt zuerst `Common`. Root-Reactor-Build sowie isolierter Client-Raspi/Portal-Build weiterhin grün, keine Quelländerung an Common/Client-Raspi/Portal. Details in 05-migration-plan.md, 03-modules.md |
| 2026-07-20 | **Phase 2 AP3 (Auth: Argon2id + SHA1-Migrationspfad, Login-/Session-Handling)**: `spring-boot-starter-security` + Bouncy Castle (Argon2-Kryptoprovider) ergänzt; neues Package `backend/.../auth/` mit `PasswordVerificationService` (Format-Erkennung Argon2id vs. SHA1-Legacy, konstante Byte-Vergleiche), `ElwasysAuthenticationProvider` (fachlicher Nachfolger von Portal `SessionManager#login`), `ElwasysUserPrincipal`, `SecurityConfig` (`/actuator/health` öffentlich, sonst Login-Pflicht, Formular-Login). **Befund**: Bestandsspalte `users.password` (`VARCHAR(50)`) zu klein für Argon2id-Hashes (empirisch 97 Zeichen) – additive Flyway-Migration `V2` auf `VARCHAR(255)` erweitert (abwärtskompatibel, siehe 05-migration-plan.md). SHA1-Altformat exakt nachvollzogen (`Utilities#sha1`: reines SHA-1, Hex-Kleinbuchstaben, kein Salt) und durch Parity-Test gegen die echte Alt-Code-Routine bewiesen. **Re-Hash-Migration hinter Flag `elwasys.auth.rehash-on-login` (Default AUS)**, weil ein Re-Hash denselben Nutzer im weiterhin SHA1-vergleichenden Alt-Portal aussperren würde (Parallelbetrieb) – vollständig implementiert und getestet, Aktivierung erst beim Portal-Cutover (Phase 3). **Beobachtung**: Alt-Portal-Login prüft `isBlocked()` nicht (nur Terminal-Kartenlogin tut das) – das neue Backend weist gesperrte Nutzer dagegen bewusst aktiv ab (Auftrag AP3, dokumentierte Abweichung vom exakten Alt-Verhalten). 52/52 Backend-Tests grün (25 neu). Root-Reactor-Build sowie isolierter Client-Raspi/Portal-Build weiterhin grün, keine Quelländerung an Common/Client-Raspi/Portal. Details in 05-migration-plan.md, 03-modules.md |
| 2026-07-20 | **Phase 2 AP4 (REST-API v1 + Standort-Token-Auth + WebSocket-Endpunkt für Terminals)**: additive Migration `V3__create_terminal_tokens.sql` (neue Tabelle, keine Bestandsänderung); `TerminalTokenService` (Hash-statt-Klartext, Rotation über mehrere aktive Tokens je Standort) + `TerminalTokenCliRunner` (Profil `token-cli`) als Phase-2-Verwaltungspfad (kein Admin-UI vor Phase 3); eigene zustandslose `SecurityFilterChain` für `/api/v1/**` (`Authorization: Bearer <token>`, `@Order(1)`, AP3-Kette unverändert) - dabei drei nicht offensichtliche Fallstricke gefunden und behoben (globale Filter-Auto-Registrierung, `securityMatcher(String)` braucht MVC-Introspector-Bean die unter `webEnvironment=NONE` fehlt, Spring-Scheduler-Threads sind nicht daemon und hielten den CLI-Prozess am Leben). REST-API v1 (Kartenlogin, Geräte-/Programmliste, Execution-Lebenszyklus, Guthaben) mit DTOs statt Entity-Serialisierung, RFC-7807-`ProblemDetail`-Fehlerbildern und striktem Standort-Scope (404 statt 403, vgl. C16); springdoc-openapi-Dokumentation unter `/v3/api-docs`/`/swagger-ui.html`. WebSocket-Fundament unter `/api/v1/terminal-ws` (JSON-Protokoll mit Typ-/Versionsfeld, HELLO/HELLO_ACK, PING/PONG-Heartbeat, STATUS_REQUEST/STATUS_RESPONSE als Gerüst, Verbindungsregistry) - volle Fernwartungs-Portierung folgt Phase 3/4. 96/96 Backend-Tests grün (44 neu). Root-Reactor-Build sowie isolierter Client-Raspi-/Portal-Build weiterhin grün, keine Quelländerung an Common/Client-Raspi/Portal. Details in 05-migration-plan.md, 02-data-model.md, 03-modules.md, 04-build-and-run.md |
| 2026-07-21 | **Phase 3 AP4 (Dialoge/Funktionen: Passwort ändern/zurücksetzen, UserSettings, ExpiredExecutions, Log-Viewer, Fernwartung) – letztes Arbeitspaket der Phase-3-Roadmap „Dialoge/Funktionen“**: eigenes Passwort ändern (P16, `ChangePasswordDialog`, neuer `PasswordService`, Passwort-Menü in `UserMenuBar` zurück), UserSettings (P17, `UserSettingsDialog`, `UserService#updateOwnSettings`), Passwort per Email zurücksetzen (P19, `PasswordForgotDialog` + neue öffentliche Route `ResetPasswordView` + Admin-Reset in `UserFormDialog`, neuer `PasswordResetService` – wiederverwendet bewusst die bestehenden `users.password_reset_key`/`-timeout`-Spalten statt einer neuen Migration; eigener Schalter `elwasys.password-reset.enabled`, Default AN, bewusst getrennt von `elwasys.notifications.enabled`), ExpiredExecutions (`ExpiredExecutionsDialog`, Warndreieck in `AdminUsersView`), Log-Viewer/Fernwartung über den bestehenden Backend-WS-Kanal (`AdminDashboardView`-Toolbar, neuer `TerminalMaintenanceService`, WS-Protokoll additiv erweitert um Portal-initiierte `LOG_REQUEST`/`RESTART_REQUEST` + neues `RESTART_RESPONSE`; Alt-TCP-Protokoll bewusst NICHT portiert, Alt-Portal bleibt dafür bis zum Cutover in Betrieb). 17 neue Tests (Backend-Suite **161/161** grün), keine neue Flyway-Migration, keine Quelländerung an Common/Client-Raspi/Portal. Details in kb/05-migration-plan.md, kb/03-modules.md, kb/02-data-model.md |
| 2026-07-21 | **Phase 3 AP5 (Live-Updates zwischen Sessions) – letztes Arbeitspaket vor der Playwright-Portierung (AP6)**: ersetzt die `events/`-Listener + Vaadin-Push-Mechanik des Alt-Portals – **Befund**: das Alt-Portal aktiviert trotz `vaadin-push`-Dependency NIRGENDS `@Push`, die `events/I*UpdatedEventListener` sind reine SAME-SESSION-Callbacks (Dialog→aufrufende View, kein Cross-Session-Update). Neues, Vaadin-freies `events`-Package (7 `DomainEvent`-Records); alle 7 Fachlogik-Services (`UserService`, `UserGroupService`, `DeviceService`, `ProgramService`, `LocationService`, `CreditService`, `ExecutionService`) publizieren nach jeder erfolgreichen Änderung über `ApplicationEventPublisher` – bewusst in der Service-Schicht, damit auch über die REST-API von Terminals ausgelöste Änderungen (`ExecutionController`) dieselben Ereignisse feuern. Neuer `UiBroadcaster` (`ui/push/`) verteilt über `@TransactionalEventListener(AFTER_COMMIT)` + `UI#access(...)` an alle registrierten Vaadin-Sessions; `@Push` auf neuer `ElwasysAppShell`-Klasse aktiviert (WebSocket + XHR-Fallback). Security-/Pfad-Koexistenz mit Terminal-WS (`/api/v1/terminal-ws`) und `VaadinSecurityConfigurer` verifiziert (disjunkte Pfade, Push-Handshake als Vaadin-interne Anfrage automatisch freigegeben), Produktionsmodus-Build (`mvn clean package -Pproduction`) weiterhin grün – kritisch, da `@Push` das Frontend-Bundle ändern kann. Admin-Dashboard (gezieltes Nachladen nur des betroffenen Geräte-Panels), die 5 Stammdaten-Grids und UserDashboard (eigenes Guthaben, gefiltert auf die eigene Benutzer-Id) abonnieren passende Ereignistypen mit sauberem attach/detach-Lifecycle. 12 neue Tests (`UiBroadcasterTest` 4, `DomainEventsTest` 8 – Backend-Suite **173/173** grün), keine neue Flyway-Migration, keine Quelländerung an Common/Client-Raspi/Portal. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-21 | **Phase-4-Planung**: Roadmap „Terminal-Modernisierung“ in 6 sequenzielle Arbeitspakete zerlegt (AP1 Sicherheitsnetz/deCONZ-Sim/ui-small-Smoke → AP2 JavaFX+`java.net.http` → AP3 Backend-API-Erweiterungen/Benachrichtigungs-Anbindung → AP4 Client-Cutover auf REST+Token → AP5 Fernwartung umdrehen → AP6 Offline-Robustheit); vorläufige Festlegungen zu den Offline-Detailfragen dokumentiert (kb/05, Abschnitt „Arbeitspakete Phase 4“). Umsetzung durch Implementierungs-Agenten, QA-Review am Phasenende |
| 2026-07-21 | **Phase 3 AP6 (Playwright-E2E-Suite P1–P20 portiert + Alt-Portal als E2E-Ziel stillgelegt) – letztes Arbeitspaket, Phase 3 damit abgeschlossen**: neue Suite `backend/e2e/` (Node/TS, analog zu `Portal/e2e/` aufgebaut) startet PostgreSQL + eine frische, dedizierte E2E-Datenbank, baut/startet das Backend-Jar im Produktionsmodus und fährt alle 20 Testfälle P1–P20 aus kb/08-test-plan.md gegen das neue Vaadin-Flow-Portal – inklusive **P11** ("Gerät aktiv/inaktiv schalten"), das in der Alt-Suite nie umgesetzt worden war. Selektor-Strategie auf Vaadin Flow umgestellt (`getByLabel`, `vaadin-dialog-overlay`, wichtigster Fallstrick: `vaadin-grid`-Zellen liegen im Light-DOM und sind keine `<tr>`-Nachkommen – Details in kb/06-ui-tests.md). Dokumentierte, bewusste Abweichungen wie in kb/05 vorgesehen behandelt: P14 navigiert über den eigenen "Standorte"-Menüpunkt statt eines Dashboard-Dialogs, P16 testet nur noch den Neu-Portal-Teil (Argon2id statt SHA1), P19 zusätzlich mit echtem Fehlerfall (kein Crash). **20/20 grün, mindestens 5 vollständige Läufe ohne einen einzigen Fehlschlag/Retry.** Nebenbefund/Bugfix: Login-Seite hatte den "Passwort vergessen?"-Knopf beim Vaadin-Default "Forgot password" (Englisch) hängen lassen – behoben. Alt-Portal-Modul aus dem CI-Playwright-Pfad genommen (`ci.yml`: alter `portal`-Job raus, neuer `backend-e2e`-Job + schlanker `portal-legacy-build`-Job, der das Alt-Portal-Modul nur noch baut), bleibt aber wie geplant bis Phase 5 im Repo; die Cross-Component-Suite (P21/P22, `client`-CI-Job) läuft unverändert weiter (hängt nur an `Common`, nicht am Portal-Modul). Regressionscheck: Backend-Suite **173/173** grün, Root-Reactor-Build grün. Details in kb/05-migration-plan.md, kb/06-ui-tests.md, kb/03-modules.md |
| 2026-07-21 | **Phase 4 AP1 (Sicherheitsnetz ausbauen) – erstes Arbeitspaket der Phase-4-Roadmap, noch gegen den unveränderten Ist-Stand des Clients**: neuer `DeconzSimulator` (`Client-Raspi/src/test/.../application/deconzsimulator/`, JDK-`HttpServer` + selbst geschriebener minimaler RFC-6455-WebSocket-Server, keine neue Abhängigkeit, JSON über die Produktions-Model-Records aus `devices/deconz/model/`) bildet den deCONZ-REST+WebSocket-Gateway-Pfad nach, analog zum bestehenden `FhemSimulator`. Drei neue E2E-Testklassen (`ClientUsageDeconzE2ETest`, `ClientAutoEndDeconzE2ETest`, `ClientAbortExecutionDeconzE2ETest`) fahren die Kernszenarien Karten-Login/Buchung/Programmstart (C2–C5), Auto-Ende per Leistungsmessung (C11) und Abbruch (C12) als deCONZ-Pendants zu den bestehenden fhem-Tests (eigene Testklassen statt Parametrisierung, damit die bewährte fhem-Suite unangetastet bleibt). Neue `ClientSmallUiSmokeE2ETest`: erste E2E-Abdeckung für `ui/small` (320×240, bislang deckte die Suite nur `ui/medium` ab) – **Befund** (dokumentiert, kein Bug): `ui/small` hat einen zu `ui/medium` umgekehrten Bedienfluss (erst Gerätewahl, dann Kartenscan). Kein Produktivcode geändert. Client-Suite **37/37 → 46/46** (`run-ui-tests.sh`), **19/19 → 28/28** (`run-client-e2e.sh`), Cross-Component unverändert **3/3** (`run-cross-component-e2e.sh`). Details in kb/05-migration-plan.md, kb/06-ui-tests.md |
| 2026-07-21 | **Phase 4 AP2 (Client-Unterbau modernisieren: JavaFX/Logback/SLF4J, Gateway-HTTP auf `java.net.http`) – zweites Arbeitspaket der Phase-4-Roadmap**: JavaFX 20 → **23.0.2** (höchste stabile, per Bytecode-Inspektion verifiziert noch Java-21-lauffähige Version – JavaFX 24+ verlangt bereits JDK 22+), SLF4J 1.7.12 → **2.0.18**, Logback 1.2.9 → **1.5.38** (beide als direkte Client-Raspi-Dependencies, Common/Portal unverändert). **Abweichender Befund**: `devices/deconz/` war REST-seitig bereits auf `java.net.http` (seit dem allerersten deCONZ-Commit von 2023), der WS-Teil nutzte schon Springs `StandardWebSocketClient`, `FhemDevicePowerManager` verwendet gar kein HTTP (reines Telnet) – für beide Gateways war keine Codeänderung nötig. Die einzige tatsächliche unirest/HttpComponents-Nutzung im Produktivcode steckte im (laut Notification-Inventar bereits als nicht portiert/tot dokumentierten) elwaApp-Push-Zweig von `ExecutionFinisher`, der 1:1 verhaltenserhaltend auf `java.net.http`+Gson migriert wurde. Damit `unirest-java`, alle drei `org.apache.httpcomponents`-Artefakte und `org.json` aus `Client-Raspi/pom.xml` entfernt (verifiziert per `mvn dependency:tree`). Baseline- und Endstand-Suiten identisch: `run-ui-tests.sh` **46/46**, `run-client-e2e.sh` **28/28**, `run-cross-component-e2e.sh` **3/3** (ein `InactivitySchedulerTest`-Einzelfehlschlag nur in der Baseline, beim gezielten Wiederholen sofort grün – bekannter Timing-Flake, keine Regression); Root-Reactor-Build und `Client-Raspi/pom.xml package` grün, kein Diff an Common/Portal/backend. Details in kb/05-migration-plan.md, kb/03-modules.md |
| 2026-07-21 | **Phase 4 AP3 (Backend-Vorbereitung für den Terminal-Cutover, additiv API v1) – drittes Arbeitspaket der Phase-4-Roadmap**: Inventur aller `DataManager`-/Direkt-DB-Aufrufe des Client-Alt-Codes gegen die REST-API v1 (13 Zugriffspunkte, 5 bereits abgedeckt, 1 ohne REST-Äquivalent nötig [ersetzt durch AP5], 7 additiv geschlossene Lücken, siehe kb/03-modules.md). Neuer, anonymer `GET /api/v1/devices/overview` (Geräteauswahl vor Kartenlogin, Gateway-Konfiguration, Wiederaufnahme-Scan/C13 über `runningExecutionId`, „letzter Benutzer"); `DeviceDto` um Gateway-Konfigurationsfelder ergänzt. Idempotenz-Schlüssel (`Idempotency-Key`-Header, additive Migration `V4__create_terminal_idempotency_keys.sql`) + optionale Original-Zeitstempel (`clientTimestamp`) für alle vier Execution-Endpunkte – zwei echte Fallstricke dabei gefunden und behoben (409-bei-Replay durch falsch platzierten „bereits beendet"-Wächter; ein wirkungsloser Mockito-Mock ohne Zustand). Neuer `GET /api/v1/snapshot` für die Offline-Buchungs-Vorbereitung (Standort-Daten ohne Passwort-Hashes, auf die am Standort zugelassenen Gruppen beschränkt). `NotificationService` an `finish`/`abort` angebunden (weiterhin hinter `elwasys.notifications.enabled`, Default AUS – Scharfschaltung bleibt AP4 vorbehalten). Kein bestehender Endpunkt-Vertrag geändert. 22 neue Tests (Backend-Suite **173/173 → 195/195**), Produktionsmodus-Build + Root-Reactor weiterhin grün, Portal-E2E **20/20** weiterhin grün (inkl. vollem Produktions-Build gegen eine frisch per Flyway inkl. `V4` migrierte DB). Details in kb/05-migration-plan.md, kb/03-modules.md, kb/02-data-model.md |
| 2026-07-21 | **Phase 4 AP4 (Client-Cutover auf die REST-API, Kernstück) – viertes Arbeitspaket der Phase-4-Roadmap, Terminal spricht keine direkte DB mehr (außer transitional für Maintenance)**: neue `api/ApiClient`- + `model/Client*`-Schicht ersetzt `Common.DataManager` in `ElwaManager`/`ExecutionManager`/`ExecutionFinisher`/den UI-Controllern; `elwasys.properties`/`setup.sh` fragen `backend.url`/`backend.token` statt DB-Zugangsdaten ab; Benachrichtigungsversand (Commons-Email/Pushover) aus dem Client entfernt (Backend versendet zentral, Scharfschaltung von `elwasys.notifications.enabled` bleibt ein operativer Schritt bei der Phase-6-Produktivumschaltung). Fernwartungs-Registrierung bleibt planmäßig bis AP5 auf dem Alt-DB-Pfad. **Kernfund bei der Fertigstellung** (ein erster Agent hatte den Cutover zu ~90% umgesetzt, aber ohne grüne Tests abgebrochen): die Testharness baute den Test-Backend-Jar ohne das `production`-Maven-Profil, wodurch Vaadins Online-Lizenzcheck (siehe Phase-3-AP1-Befund) nach ~60s den kompletten Spring-Kontext einriss – erklärt das deterministische Wegbrechen der `run-client-e2e.sh`-Suite ab der 7. Testklasse (Zeitbombe, kein Speicherleck). Fix: `-Pproduction` beim Backend-Testjar-Build (dieselbe bereits für `backend/e2e/` etablierte Umgehung); zusätzlich fehlte `run-cross-component-e2e.sh` das Backend-Token-Seeding, nachgezogen. **28/28** (`run-client-e2e.sh`, 2× reproduziert – identisch zur AP1-Zahl, keine Tests verloren), **46/46** (`run-ui-tests.sh`, bekannter Timing-Flake `InactivitySchedulerTest` isoliert grün), Cross-Component **3/3**, Backend-Suite **198/198**, Root-Reactor-Build grün. Details in kb/05-migration-plan.md, kb/03-modules.md, kb/06-ui-tests.md |
| 2026-07-21 | **Phase 4 AP5 (Fernwartung umdrehen) – fünftes Arbeitspaket der Phase-4-Roadmap, letzter Direkt-DB-Zugriff des Terminals entfernt**: neues Client-Package `ws/` (`TerminalWebSocketClient`, Technologie `StandardWebSocketClient`/`TextWebSocketHandler` wie bereits `devices/deconz/DeconzEventListener`) baut eine ausgehende, dauerhafte WebSocket-Verbindung zu `/api/v1/terminal-ws` auf (dasselbe Standort-Token wie `api/ApiClient`, kein neuer Konfig-Schlüssel), sendet `HELLO`, antwortet auf Backend-`PING` mit `PONG` und bedient die drei portal-initiierten Fernwartungs-Anfragen `STATUS_REQUEST`/`LOG_REQUEST`/`RESTART_REQUEST` (Status: Version/Startzeit/laufende Ausführungen rein lokal; Log: aktuelle Logdatei; Neustart: `ElwaManager#restart()`, jetzt mit echter `RESTART_RESPONSE`-Bestätigung statt Alt-„fire-and-forget"), inkl. automatischem Reconnect mit Backoff. `application/MaintenanceServerManager` (Client als TCP-Server) und `configuration/LocationManager` (Direkt-DB-Registrierung) sind ENTFERNT (nicht nur deaktiviert) – kein `DataManager`-/JDBC-Import mehr in `Client-Raspi/src/main` (verifiziert per Grep; zwei verbleibende `SQLException`-Importe sind reine, unangetastete Signatur-Kompatibilität, keine JDBC-Nutzung). `elwasys.properties`/`setup.sh` fragen keine `database.*`/`maintenance.*`-Werte mehr ab. Backend-seitig additiv ergänzt: `TerminalMaintenanceService#requestStatus` (neu, analog zu `#requestLog`/`#requestRestart`) + `TerminalWebSocketHandler` routet ein eingehendes `STATUS_RESPONSE` jetzt wie `LOG_RESPONSE`/`RESTART_RESPONSE` zurück – die bestehende Portal-UI (Verbunden-Badge, Log/Neustart-Knöpfe) ist unverändert, da sie bereits dieselbe Bean nutzte. `locations.client_ip/-port/-uid/-last_seen` werden nicht mehr beschrieben/gelesen, bleiben aber bewusst bis Phase 5 als Bestandsspalten erhalten (keine Migration). Alte Cross-Component-Suite (`ClientMaintenanceConnectionE2ETest`, Alt-TCP-Protokoll) entfernt, ersetzt durch `TerminalMaintenanceRealClientE2ETest` (Backend-Modul: echte `TerminalMaintenanceService`-Bean + ein echter, als Subprozess gestarteter Client-Raspi-Jar, `--module-path`/`--add-modules` für die JavaFX-Plattform-Module auf einem Standard-JDK) über neu geschriebenes `run-cross-component-e2e.sh` – **3/3 grün, 2× reproduziert**. `run-ui-tests.sh` **46/46 → 43/43**, `run-client-e2e.sh` **28/28 → 25/25** (2× reproduziert) – Rückgang um je 3 durch den Wegfall der alten `*E2ETest`-Cross-Component-Tests, deren Nachfolger jetzt im `backend`-Modul lebt. Backend-Suite **198/198 → 199/199** grün (1 neuer `requestStatus`-Unit-Test; die neue Real-Client-E2E-Testklasse läuft separat und zählt nicht mit). Root-Reactor-Build grün, kein Diff an Portal/Common, keine Bestandsspalten-Migration. Details in kb/05-migration-plan.md, kb/03-modules.md, kb/01-architecture.md, kb/02-data-model.md, kb/06-ui-tests.md |
| 2026-07-21 | **Phase 4 AP6 (Offline-Robustheit) – letztes Arbeitspaket der Phase-4-Roadmap, Phase 4 damit abgeschlossen**: additive Migration `V5__add_offline_max_duration_to_locations.sql` (`locations.offline_max_duration_minutes`, Default 60, im Portal-Standorte-Dialog editierbar, über `SnapshotDto` ans Terminal ausgeliefert). Neues Backend-Package `offline/` (`ClientTimestampPolicy`/`OfflineProperties`): akzeptiert einen vom Terminal mitgeschickten `clientTimestamp` innerhalb `offline.max-duration` + konfigurierbarer Uhren-Drift-Toleranz (Default 5min), sonst Serverzeit + Protokollhinweis; unterdrückt Replay-Benachrichtigungen zu zu alten Ereignissen. Neues Client-Package `offline/` (`OfflineSnapshotStore`/`OfflineJournal`/`OfflinePricing`/`OfflineGateway`): periodisch aktualisierter, persistenter Standort-Snapshot + neustartfestes Ereignis-Journal tragen sowohl Stufe A (laufende, online gestartete Ausführungen werden bei einem Kommunikationsfehler beim Beenden/Abbrechen lokal abgeschlossen + nachgemeldet, statt einen Fehler zu zeigen) als auch Stufe B (komplett neue Buchungen werden offline gegen den Snapshot geprüft/angelegt, solange dieser nicht älter als `offline.max-duration` ist); Replay läuft über die seit AP3 idempotenten Execution-Endpunkte und bricht bei jedem Fehler komplett ab (Journal bleibt unverändert, ein Neuversuch beginnt immer von vorn - sicher dank Backend-Dedup). `ElwaManager#cardLogin`/`#getDevicesForUser`/`#createExecution`/`#getManagedDevices` kapseln Online-Pfad + Offline-Fallback hinter demselben DTO-Vertrag wie zuvor, UI-Controller (medium+small) rufen nur noch diese Methoden auf - Normalbetrieb-Verhalten bleibt identisch. Neue Tests: `ExecutionControllerOfflineReplayTest` (Backend, 8), `ClientOfflineRobustnessE2ETest` (Client TestFX, 3, neuer `BackendProxy`-Testhelfer für gezielt simulierbare Backend-Ausfälle), `ClientOfflineReplayIdempotencyE2ETest` (Client, 1, bewusst kein TestFX). Backend-Suite **199/199 → 207/207**, Client-Suite **43/43 → 47/47** (`run-ui-tests.sh`), **25/25 → 29/29** (`run-client-e2e.sh`, 2× reproduziert), Cross-Component unverändert **3/3**, Root-Reactor-Build grün, Portal-E2E weiterhin **20/20**. Nur additive Migration, `Portal/`/`Common` unangetastet. Damit sind alle sechs Arbeitspakete der Phase 4 abgeschlossen; nächster Schritt: Phase 5. Details in kb/05-migration-plan.md, kb/03-modules.md, kb/02-data-model.md |
| 2026-07-21 | **Phase 4 CI-Stabilität (nach PR-#8-Erstellung, 3 rote Client-Raspi-CI-Läufe)**: drei getrennte, nur-in-CI auftretende Fehlerursachen behoben. (1) **Testharness-Pfadbug**: `run-ui-tests.sh`/`run-client-e2e.sh` machten `cd "$(dirname "$0")"` und sourcten danach nochmals `"$(dirname "$0")/ci-support/…"` → bei CI-Aufruf `bash Client-Raspi/run-ui-tests.sh` doppelte sich der Pfad (`Client-Raspi/Client-Raspi/…` → „No such file"); Fix: relativ zum bereits gewechselten Verzeichnis sourcen. (2) **`ApiClient`-Härtung**: einmaliges Retry auf transiente `IOException` (EOF/„received no bytes"/Connection reset), echte Fehler (refused/DNS/Timeout) weiterhin sofort; sicher, weil GET idempotent + Mutationen mit Idempotency-Key; mit Regressionstest (Mini-TCP-Server). (3) **Eigentliche Ursache des deCONZ-Fehlschlags**: `ClientResumeExecutionE2ETest` seedet bewusst eine dauerhaft unfertige Ausführung (C13-Wiederaufnahme) und räumte sie nicht auf; `ElwaManager#initiate()` scannt beim Start ALLE Standort-Geräte auf unfertige Ausführungen, und da Surefire die Klassenreihenfolge dateisystemabhängig (nicht alphabetisch) wählt, landete dieser Datensatz in CI zufällig direkt vor `ClientAutoEndDeconzE2ETest` → dessen Start versuchte, ein rein-fhem-Gerät (`deconz_uuid=''`) über den deCONZ-Gateway zu schalten → leere Id routet auf keinen Sim-Pfad (404) → `DeconzException` → Init-Absturz, nie SELECT_DEVICE. Fix: der Verursacher-Test räumt seine Fixture selbst auf (`@AfterAll`); zusätzlich Produktionshärtung in `DeconzDevicePowerManager` (klarer Fehler bei leerer deCONZ-Id statt sinnloser Retry-Kette; `getState` nutzt `isBlank` statt `== null`, da die DB „kein deCONZ" als Leerstring speichert). Lokal 49/49 grün (CI ist der eigentliche Prüfstein, da nur dort reproduzierbar). |
| 2026-07-21 | **Phase 4 CI-Stabilität – Korrektur/Abschluss des deCONZ-Fehlschlags (Runden 4–5, jetzt lokal deterministisch reproduziert)**: die vorige Zuordnung (3) war unvollständig – der Verursacher war NICHT `ClientResumeExecutionE2ETest` (Gerät `E2E-WM-Resume`), sondern **`ClientUsageE2ETest`** (Gerät **`E2E-Waschmaschine`**), das eine unfertige Ausführung hinterlässt. In der (dateisystemabhängigen) CI-Surefire-Reihenfolge lief `ClientUsageE2ETest` vor `ClientAutoEndDeconzE2ETest`; dessen standortweiter Start-Wiederaufnahme-Scan versuchte, dieses rein-fhem-Gerät über den deCONZ-Gateway zu schalten → `DeconzException` → nie SELECT_DEVICE. **Robuster, reihenfolge-unabhängiger Fix** (statt Whack-a-Mole je Verursacher): die drei deCONZ-Testklassen (`ClientUsageDeconzE2ETest`, `ClientAutoEndDeconzE2ETest`, `ClientAbortExecutionDeconzE2ETest`) löschen vor dem App-Start unfertige Ausführungen auf Geräten OHNE deCONZ-Id am Standort (`DELETE … WHERE finished=false AND … (deconz_uuid IS NULL OR deconz_uuid='')`) – genau die, an denen ihr deCONZ-Gateway sonst scheitert; sie nehmen beim Start selbst nichts wieder auf, das Löschen ist gefahrlos. **Lokal deterministisch bewiesen** über erzwungene Reihenfolge (`-Dsurefire.runOrder=reversealphabetical`, Usage vor AutoEndDeconz): OHNE Fix rot (exakt der CI-Fehler, 46,78 s Timeout), MIT Fix grün (5/5, BUILD SUCCESS). Die ResumeTest-`@AfterAll`-Bereinigung + `DeconzDevicePowerManager`-Härtung aus der vorigen Runde bleiben (sinnvoll, aber allein nicht ausreichend). Kein Produktivcode in dieser Runde geändert (nur Testcode). |
| 2026-07-21 | **Phase 4 abgeschlossen (QA-Review durch den Koordinator, ohne blockierende Befunde)**: Diff-Review aller 23 Phase-4-Commits (139 Dateien, ~9.645 Zeilen) gegen CLAUDE.md/Roadmap/Auftraggeber-Entscheidungen; Kern-Rahmenbedingung bestätigt (`git diff cca8f60..HEAD -- Portal Common` LEER – kein Alt-Modul angefasst). Stichproben-Tiefenprüfung der geld-/sicherheitskritischen Pfade: Backend-Idempotenz (Replay liefert gespeicherte Antwort ohne erneute fachliche Aktion; dokumentierte Race-Grenze für Single-Writer-Terminal akzeptabel), Offline-Journal (append-only, korruptionstolerant, Guthaben-Delta neustartfest aus dem Journal), `OfflineGateway` (konservativ: gesperrte Nutzer abgelehnt, Guthabenprüfung gegen `priceAtMaxDuration`, veralteter Snapshot → C15-Fehlerbild), **`SnapshotUserDto` enthält verifiziert KEIN Passwortfeld**, `ClientTimestampPolicy` (±5min-Toleranz + Max-Dauer + Benachrichtigungsunterdrückung exakt nach Festlegungen), `V5` rein additiv (`ADD COLUMN NOT NULL DEFAULT 60`), Notification-Gating Default AUS (`ELWASYS_NOTIFICATIONS_ENABLED:false`). **Unabhängig nachgefahrene Suiten**: Backend **207/207**, `run-client-e2e.sh` **29/29**, plus die von den AP-Agenten belegten `run-ui-tests.sh` 47/47, Cross-Component 3/3, Portal-E2E 20/20. Nicht-blockierende, dokumentierte Restrisiken (für Phase 5/6 vermerkt): Offline-Neustart mitten in laufender Ausführung kann diese nicht wiederaufnehmen (C13 braucht Backend); zwei vestigiale `java.sql.SQLException`-Importe im Client (Aufräumkandidat Phase 5); Replay bricht bei fachlichem Fehler komplett ab (bewusste Vereinfachung). Kein Fix-Auftrag nötig – Phase 4 gilt als abgeschlossen. |
| 2026-07-21 | **Phase 5 AP1 (Alt-Portal-Modul + `Common.DataManager`/Maintenance-Altprotokoll entfernt) – erstes Arbeitspaket der Phase-5-Roadmap**: `Portal/`-Modul komplett aus dem Repo entfernt (inkl. `portal-legacy-build`-CI-Job); `Common` von 17 auf 6 Klassen geschrumpft (`ProgramType`, `Utilities`, `FormatUtilities`, `ConfigurationManager`, `LocationOccupiedException`, `NoDataFoundException` bleiben, `resources/` unangetastet) – entfernt: `DataManager`, die Alt-Entities, `DiscountType`, `NotEnoughCreditException`, das komplette `maintenance/`-Paket. Root-Reactor jetzt 3 Module. Mit-entfernt: dadurch blockierte Backend-Parity-Tests (`CreditServiceParityTest`, `PricingServiceParityTest`, `LegacyDataManagerFactory`) sowie zwei gegenstandslose, nie in den Suiten laufende Maintenance-Test-Dateien in Common/Client-Raspi. Testzahlen: Backend **207/207 → 197/197**, Client-UI/Cross-Component unverändert. Details in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-21 | **Phase 5 AP2 (DB härten: DB-Rollen + Default-Passwörter)**: neue additive Flyway-Migrationen `V6__harden_db_roles.sql` (entfernt die Alt-Rollen `elwaclient1`/`elwaapi` + Gruppe `elwaclients` samt Default-Passwörtern `elwaclient1`/`api1234`; `elwaportal` bleibt einziger Anwendungs-DB-User – robust/idempotent, fängt die cluster-weite Rollen-Abhängigkeit im geteilten Test-Cluster ab) und `V7__remove_default_admin_password.sql` (leert `users.password` des Seed-`admin`, aber nur wenn er noch den unveränderten Default-SHA1-Hash trägt – Bestandsinstallationen mit geändertem Passwort unberührt). Neues Admin-CLI `AdminPasswordCliRunner` (Profil `admin-cli`, analog `token-cli`) setzt via `PasswordService#setNewPassword` (Argon2id) das Passwort eines bestehenden Benutzers – auf frischen Installationen der einzige Weg, dem `admin` überhaupt ein Passwort zu geben (bewusste Verhaltensänderung nur für Neuinstallationen, Bestand unberührt). `backend/e2e/scripts/start-backend.sh` seedet `admin`/`admin` jetzt über dieses CLI (zwei-Schritt wie token-cli); `ClientAppE2ETest` von der als `elwaclient1` verbindenden, seit Phase 4 obsoleten Standort-Reset-Setup-Methode befreit; `TerminalWebSocketConfig` schließt jetzt auch `admin-cli` vom Heartbeat-Scheduler aus (sonst beendet sich der CLI-Prozess nicht). Alle Suiten grün: Backend **197 → 200** (3 neue `AdminPasswordCliRunnerTest`), Client-UI 49/49, Playwright-E2E **19/19 (P1–P20, admin-Login weiter grün)**, Cross-Component 3/3. Details in kb/05-migration-plan.md (Änderungslog), kb/02-data-model.md, kb/04-build-and-run.md |
| 2026-07-21 | **Phase 5 AP3 (Spaltentypo `auto_end_power_threashold` → `auto_end_power_threshold` fixen + obsolete `locations.client_*`-Fernwartungsspalten entfernen)**: zwei additive Flyway-Migrationen, `V8__rename_auto_end_power_threshold.sql` (Spaltenrename in `devices`, im selben Commit durch Backend-`DeviceEntity`/DTOs/Services/UI und Client-DTOs/`ClientDevice`/`ExecutionManager`/`OfflineGateway` gezogen – Wire-Contract auf beiden Seiten synchron umbenannt) und `V9__drop_obsolete_location_client_columns.sql` (`locations.client_uid`/`client_ip`/`client_port`/`client_last_seen`, seit Phase 4 AP5 ungenutzt). Test-Seeds in 13 Client-E2E-Tests + `DeviceControllerTest` + `dashboard.spec.ts` nachgezogen. Alle vier Suiten unverändert grün: Backend 200/200, Client-UI 49/49, Playwright-E2E 19/19 (P1–P20), Cross-Component 3/3. Details in kb/05-migration-plan.md (Änderungslog), kb/02-data-model.md. |
| 2026-07-21 | **Phase 5 AP4 (App-Reste `elwaapi` entfernen)**: eine additive Flyway-Migration `V10__drop_app_remnants.sql` entfernt den Trigger `user_authkey_trigger` + die Funktionen `user_authkey_trigger_function()`/`generate_user_authkey()`, die `users`-Spalten `app_id`/`access_key`/`auth_key`, die Tabellen `reservations`/`foreign_authkeys` sowie (nach verifizierter Ungenutztheit im Java-Code) die Config-Schlüssel `authkey.prefix`/`reservation.duration` – alles Reste der mobilen App, die laut Auftraggeber nicht mehr relevant ist (Entscheidung 2026-07-20). Terminal-UI (medium): die seit Phase 4 tote Auth-Key-Anzeige wurde aus `ConfirmationViewController`/`UserSettingsViewController` + `ConfirmationPane.fxml`/`UserSettingsPane.fxml` entfernt (FXML↔Controller-Konsistenz gewahrt, keine verwaisten `fx:id`/`onAction`-Referenzen); small-UI hatte nie eine solche Anzeige. Backend-Javadoc (`UserEntity`, `NotificationService`) aktualisiert. Alle vier Suiten unverändert grün: Backend 200/200, Client-UI 49/49 (schärfster Test für FXML↔Controller-Konsistenz), Playwright-E2E 19/19 (P1–P20), Cross-Component 3/3. Details in kb/05-migration-plan.md (Änderungslog), kb/02-data-model.md. |
| 2026-07-21 | **Phase 5 AP5 (Release-Pipeline final)**: `maven-publish.yml` (Release-Workflow) veröffentlicht wie bisher Terminal-fat-jar (Release-Asset) + Backend-Image (GHCR); AP5 hat die AP1-Portal-Reste beseitigt und den Job gehärtet. **Kritischer Fix**: `backend/Dockerfile` kopierte noch `Portal/pom.xml` (seit AP1 nicht mehr vorhanden) → Image-Build (CI-`backend`-Job + Release) wäre gescheitert; `COPY`-Liste + Kommentare auf die drei Reactor-Module korrigiert, `.dockerignore` analog bereinigt. Release-Build `mvn install -pl Common,Client-Raspi -am` um `-DskipTests` ergänzt (Release-Runner hat kein Xvfb/PG/Backend – Tests sind CI-Sache; lokal verifiziert, fat-jar korrekt erzeugt). `actions/checkout`/`setup-java` v3 → v4. Keine Suiten-relevante Codeänderung, nur Build-/Release-Infra; Root-Reactor grün, beide Workflow-YAMLs validiert. Details in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-21 | **Phase 5 AP6 (Doku-Endstand: READMEs/kb/Setup auf Zielarchitektur) – letztes Arbeitspaket der Phase-5-Roadmap, damit alle sechs Phase-5-APs umgesetzt** (formale QA-Review der Phase folgt separat durch den Koordinator): reine Doku-/Kommentar-Bereinigung (KEINE Java-Produktions-/Testlogik geändert, `mvn install -DskipTests` weiterhin BUILD SUCCESS). Root-`README.md` auf die Zielarchitektur gebracht (Components-Abschnitt: Client-Raspi/backend/Common, kein Portal-Modul mehr). `kb/00-overview.md`/`kb/01-architecture.md`/`kb/03-modules.md`/`kb/06-ui-tests.md` (+ kurze Klarstellungen in `kb/04`/`kb/07`/`kb/08`) auf den Zielarchitektur-Endstand gebracht – Alt-Portal/Alt-TCP-Maintenance/Terminal-Direkt-DB-Zugriff durchgängig als „entfernt (Phase 5 AP1)"/„abgelöst (Phase 4)" markiert statt weiter als aktive Komponenten beschrieben. Stale Kommentare in `backend/src/main/resources/application.yml`, `AuthProperties`/`NotificationsProperties`-Javadoc, `Client-Raspi/pom.xml`/`run-ui-tests.sh`/`run-client-e2e.sh`, `backend/pom.xml`, `deploy/helm/elwasys-backend/{Chart.yaml,values.yaml}`, `deploy/compose/.env.example` und `backend/verify-schema-baseline.sh` (Datei-Kopf als historisches Werkzeug richtiggestellt, Skript-Logik unverändert) korrigiert. **Zusatzfund**: `CLAUDE.md` war seit vor Phase 5 nicht mehr aktualisiert worden (Aktueller-Stand-Absatz auf altem Phase-4-Stand, Build-Block enthielt den kaputten Befehl `mvn -f Portal/pom.xml package`) – korrigiert. `Client-Raspi/setup.sh`/`elwasys.example.properties` gegen den Code verifiziert (grep über `WashguardConfiguration#getProperty`): enthalten bereits ausschließlich tatsächlich gelesene Schlüssel, keine Änderung nötig; `portalUrl` ist entgegen der Auftragsvermutung weiterhin aktiv genutzt (Anzeige auf dem Terminal-Bestätigungsbildschirm) und blieb bewusst erhalten. Rest-Grep über das gesamte Repo bestätigt: keine verbliebene Nennung des Alt-Portals als aktives Modul mehr. Details/vollständige Dateiliste in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-22 | **Phase 6 AP1 (Produktiv-Migrationswerkzeuge)**: neues Verzeichnis `deploy/cutover/` mit den Cutover-DB-Werkzeugen – `01-preflight-check.sh` (rein lesend: Flyway-Status, Alt-Artefakte, Dateninventar, Warnungen), `02-issue-terminal-tokens.sh`/`03-set-admin-password.sh` (dünne Wrapper um `token-cli`/`admin-cli`), `04-review-obsolete-locations.sql` (read-only Review), `verify-cutover-migration.sh` (wartbares Kern-Verifikationsskript: Testkopie des Alt-Weg-Schemas, Bestandsdaten einfügen, Backend-Jar dagegen starten, 21 Asserts über Flyway-Historie/Datenerhalt/Schema-Härtung) + `README.md` (Runbook). Tatsächlich lokal ausgeführt: **21/21 Asserts PASS**. Details in kb/05-migration-plan.md (Änderungslog), kb/04-build-and-run.md. |
| 2026-07-22 | **Phase 6 AP2 (Rollback-/Rückbau-Skript)**: `deploy/cutover/rollback-cutover.sql` (idempotentes Reverse-DDL, macht V3..V10 rückgängig, V2 bewusst unangetastet) + `rollback-cutover.sh` (Wrapper, braucht CREATEROLE-Verbindung für die V6-Umkehrung) + `verify-rollback.sh` (Verifikation: Alt-Schema wieder da, Geschäftsdaten erhalten, Idempotenz-Beweis per zweitem Lauf, Re-Cutover-Beweis per erneutem Backend-Start). Tatsächlich lokal ausgeführt: **29/29 Asserts PASS (1. Lauf), 29/29 PASS (Idempotenz-Wiederholung), 4/4 PASS (Re-Cutover-Beweis)**. `README.md`-Abschnitt "Rollback" ausgebaut (Backup-Restore vs. Reverse-DDL, Caveats). Details in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-22 | **Phase 6 AP3 (Terminal-Neuaufsetzung + Java-21-Upgrade-Pfad + run.sh-Supervisor)**: `Client-Raspi/setup.sh` generiert `run.sh` jetzt als **Supervising-Loop** (kein systemd) – Endlosschleife startet das per Symlink `raspi-client.latest.jar` verlinkte Jar im Vordergrund (unveränderte JavaFX-App/Bedienfluss, gleiche Flags/Log-Ausgabe), relauncht nach JVM-Ende den dann aktuell verlinkten Jar; `sudo killall java`-Cleanup nur EINMALIG vor der Schleife. **Supervisor-Vertrag (Grundlage AP4/AP5):** externer Neustart == laufenden `java`-Prozess beenden, Loop relauncht automatisch – ein Update hängt nur den Symlink um. Neu `deploy/terminal/`: `upgrade-jre.sh` (Nachrüst-Pfad für Java-17-Altgeräte: `bellsoft-java21-runtime-full` über dieselbe apt-Quelle wie setup.sh, idempotent, verifiziert danach robust `java -version >= 21` über sourcebare Funktionen `java_major_version`/`require_java_at_least`) + `README.md` (Terminal-Runbook: Ersteinrichtung vs. Bestandsgerät, „Java 21 ZUERST", backend.url/token, Supervisor-Vertrag). **Löst das Phase-1-Java-17-Restrisiko der Risikotabelle auf.** Tatsächlich im Vordergrund ausgeführt: `bash -n` auf allen Skripten + dem generierten `run.sh` (OK); Trockentest der Supervising-Loop (Symlink A→B zwischen zwei Iterationen umgehängt, Iteration 2 las das Ziel neu → B, PASS); Trockentest der Java-Versionsprüfung (17 → Fehler, 21 → OK); Regressionscheck `Client-Raspi/run-ui-tests.sh` **grün: 49/49**. Kein Client-Java-Code geändert. Details in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-22 | **Phase 6 AP4 (Terminal-Update-Skript `update.sh`)**: neu `deploy/terminal/update.sh` (`set -euo pipefail`) hebt ein bereits provisioniertes Terminal auf ein neues Client-fat-jar, OHNE das interaktive `setup.sh` (elwasys.properties/logback.xml/run.sh/.xsession unangetastet). Bezug: `--version <tag>` (lädt `raspi-client-<tag>.jar` von der GitHub-Release-URL, gleiches Muster wie setup.sh `install_elwasys`, robust `.part`→`mv`) oder `--jar <Pfad>` (Offline). **Jar-Layout-Konvention (AP5-Rollback baut darauf auf):** `raspi-client-<version>.jar` bleiben liegen, `raspi-client.latest.jar` → aktuelle, **`raspi-client.previous.jar` → vorige Version (Rollback-Ziel)**; Reihenfolge latest-Ziel→previous, dann latest→neues Jar, dann Neustart (relative Basenames, `ln -sfn`). Neustart per Supervisor-Vertrag: läuft java, beenden (`ELWA_RESTART_CMD`, Default `sudo killall java`) → Loop startet neues Jar; kein java → Hinweis statt Fehler (`ELWA_JAVA_PGREP`, Default `pgrep -x java`). Idempotent (latest schon neu → No-op, previous unberührt); `ELWA_ROOT`-Override. `README.md` um Abschnitt „`update.sh`" ergänzt (inkl. Java-21-Voraussetzung → `upgrade-jre.sh` zuerst, Auto-Update-Rollback „folgt in AP5"). Tatsächlich im Vordergrund ausgeführt (Temp-ELWA_ROOT, Fake-Jars + gefakter Loop): latest/previous-Umschaltung + Relaunch mit neuer Version PASS, Idempotenz PASS, Kein-java-Fall PASS, Usage/Exit-1 PASS; GitHub-Download nur trocken (URL-Konstruktion). Kein Java-Change, `pgrep -a java` am Ende leer. Details in kb/05-migration-plan.md (Änderungslog). |
| 2026-07-22 | **Phase 6 AP5 (Auto-Update-Watchdog mit Rollback + `SELECT_DEVICE`-Readiness-Marker)**: bewusst schlanke Shell-/Cron-Variante (Auftraggeber-Entscheidung „kein Overkill", kein systemd, kein großer Client-Umbau). Einziger Client-Java-Zusatz: kleine Klasse `TerminalReadinessMarker` – `markReady()` schreibt eine Marker-Datei (`${user.dir}/.terminal-ready`, per System-Property `elwasys.readyMarkerFile` überschreibbar) mit frischem `mtime`; robust (IO-Fehler nur geloggt, nie in die UI). Aufruf als Einzeiler in BEIDEN `MainFormStateManager` (medium+small) beim erfolgreichen Wechsel nach `MainFormState.SELECT_DEVICE` (feuert u. a. beim `STARTUP→SELECT_DEVICE`). Neu `deploy/terminal/auto-update-watchdog.sh`: pollt Ziel-Version (GitHub-`latest`, optional `.update-target`-Override), ruft bei neuerer Version `update.sh --version`, verifiziert den Start über einen Marker-`mtime > restart_epoch` binnen `ELWA_UPDATE_DEADLINE` (Default 180s); Fehlschlag → **ROLLBACK** (`latest` zurück auf `previous` + Konfig-Snapshot + java killen + Recovery bestätigen + FAILURE-Log). Lockfile gegen parallele Cron-Läufe; stiller No-op bei up-to-date/kein-java. `README.md` um „Auto-Update mit Rollback" (inkl. Cron-Zeile) ergänzt. Tatsächlich im Vordergrund ausgeführt: Client-UI **53/53** (Baseline 49 + 4 neue Marker-Unit-Tests); Watchdog-Trocken-Tests **16/16 Asserts PASS** (Szenario A gutes Update / B Rollback+Recovery / C No-op / Lockfile), `pgrep -a java` am Ende leer; `bash -n` auf allen deploy/terminal-Skripten OK. Details in kb/05-migration-plan.md (Änderungslog), kb/03-modules.md, kb/04-build-and-run.md. |
| 2026-07-22 | **Phase 6 AP6 (Post-Deploy-Smoke-Test-Suite / Rollout-Gate für Portal/Backend)**: Portal/Backend bekommen bewusst KEIN eigenes Upgrade-/Rollback-Skript – Rollout/Rollback laufen über die Plattform (docker-compose-Redeploy bzw. Helm-Rollback). Stattdessen ein Gate, das gegen die frisch deployte, laufende Umgebung läuft; ein Rollout gilt erst nach GRÜNEM Smoke-Test als erfolgreich. Neu `deploy/smoke/post-deploy-smoke.sh` (Schritt 1 `GET $BASE_URL/actuator/health` → `status=UP` mit Retries; Schritt 2 schlanke Playwright-Teilmenge via `npm run smoke`; Exit 0 nur bei beidem grün) + `deploy/smoke/README.md`. Neu `backend/e2e/playwright.smoke.config.ts` (KEIN webServer, KEIN globalSetup, `baseURL` aus `E2E_BASE_URL` Default `http://localhost:8080`, `testDir: tests-smoke`) + `backend/e2e/tests-smoke/smoke.spec.ts` (strikt READ-ONLY, 4 Checks: Login-Seite / Admin-Login→Dashboard / Kern-Sektionen Benutzer·Geräte·Programme / Dashboard; Admin-Creds aus `SMOKE_ADMIN_USER/PASSWORD`, wiederverwendet `login()` der Haupt-Suite) + package.json-Script `smoke`. Rein additiv – Haupt-Suite (P1–P20, `playwright.config.ts`/`global-setup.ts`/`tests/`) unverändert. Tatsächlich im Vordergrund verifiziert (kein Docker-Daemon in der Sandbox → gegen Produktionsmodus-`java -jar` auf 8081): **Health UP + Playwright-Smoke 4/4 grün**, Gate-Ausgabe PASS/Exit 0; Server danach sauber gestoppt, keine `java`/`node`/`mvn`-Prozesse übrig. Details in kb/05-migration-plan.md (Änderungslog), kb/06-ui-tests.md, kb/04-build-and-run.md. |
| 2026-07-22 | **Phase 6 AP7 (Cutover-Runbook / Strangler-Reihenfolge Portal/Backend → Terminals)** – letzter Phase-6-Roadmap-Punkt „Cutover-Reihenfolge festlegen und proben". Reines Doku-AP (keine Code-/Skript-Änderung). Neu `deploy/CUTOVER-RUNBOOK.md`: das übergreifende, orchestrierende Runbook, das die AP1–AP6-Werkzeuge zu einer sequenzierten Anleitung zusammenführt und die Detail-Runbooks (`deploy/cutover`/`deploy/terminal`/`deploy/smoke`-README) verlinkt statt zu duplizieren. Strangler-Reihenfolge mit Gate/Rollback je Schritt: 3a Portal/Backend gegen die BESTEHENDE Produktiv-DB (Flyway baseline-on-migrate) → Gate `post-deploy-smoke.sh` → Plattform-/Backup-/`rollback-cutover.sh`-Rückweg; 3b Standort-Tokens + Admin-Passwort; 3c beobachten; 3d Terminals chargenweise (JRE-21 zuerst → Konfig/Jar → `SELECT_DEVICE` verifizieren); 3e Benachrichtigungen (`ELWASYS_NOTIFICATIONS_ENABLED=true`) bewusst separat/zuletzt. Plus Rollback-Entscheidungsbaum, Post-Cutover-Checkliste, Wartungsfenster-Timeline. DB-/Backend-Verkettung in der Sandbox real geprobt: `verify-cutover-migration.sh` **21/21 PASS** → `post-deploy-smoke.sh` **Health UP + Playwright-Smoke 4/4 grün** (Exit 0); Feld-/Docker-/Helm-Schritte nur dokumentiert (kein Docker-Daemon/keine armhf-Hardware). **Damit alle Phase-6-Arbeitspakete AP1–AP7 umgesetzt** – formale QA-Review der Phase steht noch aus (Koordinator). Details in kb/05-migration-plan.md (Änderungslog), kb/04-build-and-run.md. |
| 2026-07-22 | **Phase-5-Nachtrag: `common`-Modul aufgelöst (Root-Reactor 3 → 2 Module)**. Analyse ergab: der Backend-Produktivcode hatte **0** Imports auf `common` (nur 3 Auth-Parity-Tests, test-scope), nur der Terminal-Client nutzt die 6 Utility-Klassen zur Laufzeit; die ~20 gleichnamigen DTO-/WS-Klassen in beiden Modulen sind der bewusst duplizierte REST-/WS-Wire-Contract (dürfen NICHT geteilt werden, sonst Wiederkopplung Terminal↔Backend). Umgesetzt: die 6 Klassen (Package `org.kabieror.elwasys.common` unverändert) nach `Client-Raspi/src/main/` verschoben; `commons-lang3` als direkte + PostgreSQL-JDBC-Treiber als **test-scope** Client-Raspi-Dependency ergänzt (früher transitiv via common; Treiber nur für die JDBC-Seed-Harness, Terminal-Produktion greift seit Phase 4 nicht mehr auf die DB zu); geteilte Fixture `Common/resources/database-init.sql`(+`database-upgrade/`) → neutrales Top-Level **`database/`** (mit README); tote `ISO_7010_*.svg` (Alt-Portal-Reste) gelöscht; Backend-Parity-Tests nutzen den neuen Test-Helfer `LegacySha1` (reproduziert `Utilities#sha1` byte-genau) statt der common-Dependency. ~15 Skripte/CI/Hook/cloud-init auf `mvn -N install` (statt `-pl Common -am`) + neuen SQL-Pfad umgestellt; separater CI-Job „Common" entfernt. Flyway-Migrationen V1–V10 bewusst UNANGETASTET (Checksummen). Alle Suiten grün: Backend **200/200**, Client-UI **53/53**, Client-E2E **29/29**, `verify-cutover-migration.sh` **22/22 PASS** (Backend UP gegen Alt-Schema am neuen Pfad), alle Skripte `bash -n`/YAML valide. Details in kb/05-migration-plan.md (Änderungslog + Entscheidung). |
| 2026-07-22 | **Phase 5 QA-Review (ohne blockierende Befunde)** – Koordinator mit Sonnet-Review-Subagent, Korrekturen durch separate Entwickler-Agenten. Diff-Review der Phase-5-Commits (`184133b..afaf6e5` + Common-Nachtrag `f920843..95b256d`): V6–V10 idempotent/additiv, V1–V5-Checksummen byte-identisch, V7-Hash = `sha1("admin")`, `auto_end_power_threshold`-Rename beidseitig sauber, gedroppte Objekte ohne Restreferenz, `elwaportal` einziger App-DB-User, Common-Auflösung ohne Wiederkopplung. **2 MINOR + 1 NITPICK behoben**: tote `getDatabase*`-Getter + `database.*`-Properties (`elwaclient1`) aus 16 E2E-Fixtures entfernt, hängender `{@link LegacyDataManagerFactory}`-Javadoc bereinigt, Schrittnummerierung in `start-backend.sh` korrigiert. Suiten grün: Backend **200/200**, Client-UI **53/53**. Details in kb/05 (Änderungslog). |
| 2026-07-22 | **Phase 6 QA-Review** – Koordinator mit Sonnet-Review-Subagent, Korrekturen durch separate Entwickler-Agenten. DB-Migrations-/Rollback-Korrektheit (`rollback-cutover.sql` vs. V3–V10/V1, Verify-Asserts, Marker/Smoke-Isolation) ohne Befund. **1 BLOCKER + 1 MAJOR im `auto-update-watchdog.sh` behoben**: (B1) reiner Fetch-Fehler löste denselben Rollback+`java`-Kill aus wie ein Fehl-Deploy → jetzt `latest`-Symlink-Ziel-Vergleich, unverändert ⇒ nur WARNUNG+Exit; (M1) kein Leerlauf-Check vor dem Kill → fail-safe Leerlauf-Gate (`devices/overview` `occupied` + Marker-Frische, Unsicherheit ⇒ verschieben) + README-Doku. **4 MINOR/2 NITPICK**: `run.sh` hängt Logs an statt zu truncaten (+Rotation), stale Doku-Refs (`verify-schema-baseline.sh`/`database-init.sql`) bereinigt, Feldtrenner-Bug in `02-issue-terminal-tokens.sh`, `upgrade-jre.sh` vereinfacht, T1 kommentiert. Watchdog-Fixes per 31 Dry-Run-Checks belegt, `bash -n` grün; Java-berührende Änderungen: Backend **200/200**, Client-UI **53/53** (der dokumentierte deCONZ-Order-Flake trat einmal unter paralleler Suiten-Last auf, isoliert 53/53 – keine Regression). Details in kb/05 (Änderungslog). |
| 2026-07-22 | **Phase-5-Nachtrag: Alt-Schema auf eine einzige Quelle konsolidiert (`database-init.sql`-Duplikat entfernt)**. Auf Nachfrage geklärt, dass die Fixture `database-init.sql` faktisch ein byte-äquivalentes **Duplikat** der Flyway-Baseline `V1__baseline_schema_0_4_0.sql` war (empirisch per `pg_dump` bewiesen: Schema + Grants + Seed identisch; einzige Differenz = zufalls-generierte `authkey.prefix`/`auth_key`-Werte, die V10 ohnehin droppt). Zwei Kopien desselben 0.4.0-Schemas = latentes Drift-Risiko für `baseline-on-migrate`. **Umgesetzt**: `V1` ist jetzt die EINZIGE Quelle des Alt-/Basis-Schemas; die 5 Test-/Cutover-/CI-/cloud-init-Seed-Stellen (`run-ui-tests.sh`, `run-client-e2e.sh`, `ci.yml`, `cloud-config.yaml`, `verify-cutover-migration.sh`) spielen `V1` **direkt per psql** ein (DB vorher anlegen, da V1 keine `CREATE DATABASE`-Präambel hat). Das gesamte Verzeichnis `database/` (init + pre-Flyway-`database-upgrade`-Archäologie + README) und das obsolete `backend/verify-schema-baseline.sh` (dessen Zweck – die zwei Repräsentationen vergleichen – entfällt; war ein als „historisch/erwartet-fehlschlagend" dokumentiertes Phase-2-Relikt, in keiner CI) wurden entfernt. Alle Kommentar-/Doku-Verweise nachgezogen; Flyway-Migrationen V1–V10 unangetastet (Checksummen). Verhalten unverändert. Verifikation grün: Backend **200/200**, Client-E2E **29/29** (neuer `CREATE DATABASE`+V1-Seed-Pfad), `verify-cutover-migration.sh` **22/22 PASS** (Backend UP gegen die per V1 erzeugte pre-Flyway-DB), empirischer V1≡init-`pg_dump`-Beweis. Details in kb/05-migration-plan.md (Änderungslog + Entscheidung). |
| 2026-07-22 | **Portal-Design wiederhergestellt (AdminLTE-Look des Alt-Portals)** – Auftraggeber-Rückmeldung: das Vaadin-Flow-Portal (Phase 3) sah nach der Umstellung „nicht mehr schön" aus. Ursache: der Neubau hatte zwar überall CSS-Klassen-Hooks (`admin-layout-title`, `dashboard-device-panel` …), aber **nie ein eigenes Theme-Stylesheet** – es rendert daher in der nackten Lumo-Standardoptik (heller Header, weiße Sidebar, rahmenlose Tabellen). Das vertraute Erscheinungsbild (blauer Kopfbalken + weiße Schrift, dunkle Sidebar mit hervorgehobenem Aktiv-Punkt inkl. blauem Linksbalken, gerahmte/gezebra-te Tabellen, hellgrauer Inhalt, Login als Karte mit blauem Oberrand) ist wiederhergestellt. **Bewusst NICHT als kompiliertes `@Theme`**, sondern als zur Laufzeit in den `<head>` injiziertes Stylesheet (`ElwasysAppShell#configurePage` → `src/main/resources/portal-theme.css`): ein eigenes Theme/`@CssImport` würde einen anwendungsspezifischen Frontend-Bundle-Build erzwingen und damit den Vaadin-24.10-Lizenzcheck gegen vaadin.com auslösen (in der Sandbox/CI 403 → Build-Abbruch; siehe Risikotabelle/Änderungslog). Das Inline-CSS wirkt über Lumo-Custom-Properties + `::part()` bis in die Shadow-DOMs, ohne das Standard-Bundle zu verlassen. Nur Farben/Rahmen, kein verändertes Verhalten: Portal-E2E **P1–P20 grün (19/19)**, sauberer `-Pproduction`-Build aus dem Quellcode. Details in kb/05-migration-plan.md (Änderungslog), kb/06-ui-tests.md. |
| 2026-07-22 | **Portal-Design: Palette an das Terminal angeglichen + Dashboard-Karten responsiv (Auftraggeber-Rückfrage)**. (1) *Palette-Konsistenz*: Terminal und Portal nutzten NICHT dieselbe Palette – Terminal-Blau `#4488dd` (`Client-Raspi/.../ui/medium/MainForm.css`), das Alt-Portal Vaadin-7-**Valo-Standardfarben** (Blau ~`#197de1`, most overrides auskommentiert, aus der Git-Historie geprüft), die erste Wiederherstellung AdminLTE-`#3c8dbc`. Jetzt auf die **Terminal-Palette vereinheitlicht** (Blau `#4488dd`; Status-Grün/-Rot/-Grau wie das Terminal), damit beide als EIN Produkt wirken. (2) *Dashboard-Karten zu klein*: waren fest `24em` breit → Verlaufstabelle (Datum/Benutzer/Dauer/Preis) wirkte abgeschnitten. Im Alt-Portal (`Portal/.../dashboard.scss`) waren die Gerätekarten **responsiv 50 % (zwei pro Reihe) / 100 % (eine pro Reihe)**; das ist jetzt 1:1 wiederhergestellt (`AdminDashboardView`: feste Breite raus; Theme setzt `flex: 1 1 calc(50% - .5rem)` + `min-width`), plus status-farbiger oberer Kartenrand wie die Terminal-Kacheln (frei=grün, besetzt=rot, deaktiviert=grau). Nur Farben/Layout, kein Verhalten: Portal-E2E **P1–P20 19/19 grün** (sauberer `-Pproduction`-Build). Details in kb/05-migration-plan.md (Änderungslog). |
